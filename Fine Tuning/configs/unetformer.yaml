_base_: "configs/finetune_base.yaml"

model:
  name: "unetformer"
  encoder: "resnet34"
  pretrained: true
  in_channels: 3
  out_channels: 1

batch_size: 2
gradient_accumulation_steps: 8
gradient_checkpointing: true

output_dir: "outputs/unetformer"
wandb_run_name: "unetformer-resnet34"
