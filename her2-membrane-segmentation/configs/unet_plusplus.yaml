_base_: "configs/base.yaml"

model:
  name: "unet_plusplus"
  encoder: "resnet34"
  pretrained: true
  in_channels: 3
  out_channels: 1

output_dir: "outputs/unet_plusplus"
wandb_run_name: "unet_plusplus-resnet34"
